---
/**
 * Philosophy Section — فلسفة تجدد
 * Premium cinematic brand philosophy manifesto with
 * horizontal scroll pillars, animated energy circuit, and glassmorphism.
 */
import type { Locale } from '../../lib/api/types';
import { dictionary } from '../../i18n';

const { locale } = Astro.props as { locale: Locale };
const copy = dictionary(locale).home.about;
const mobileNodes = copy.mobileNodes.map((node, index) => ({
  ...node,
  logo: index === 0 ? '/logos/cnc-logo-white.svg' : index === 1 ? '/logos/tajaddod-icon.svg' : '/logos/liper-logo-white.svg',
  color: index === 0 ? 'var(--tj-green)' : index === 1 ? 'var(--tj-text-inverse)' : 'var(--tj-orange)',
  hub: index === 1,
}));
---

<section class="section philosophy-section layer-corporate" id="about">
  <!-- Background layers -->
  <div class="ph-bg" aria-hidden="true">
    <div class="ph-bg__grid"></div>
    <div class="ph-bg__aurora ph-bg__aurora--1"></div>
    <div class="ph-bg__aurora ph-bg__aurora--2"></div>
    <div class="ph-bg__aurora ph-bg__aurora--3"></div>
    <div class="ph-bg__noise"></div>
    <!-- Floating energy lines -->
    <div class="ph-bg__lines">
      <span class="ph-line" style="--top:18%;--delay:0s;"></span>
      <span class="ph-line" style="--top:42%;--delay:-2.5s;"></span>
      <span class="ph-line" style="--top:67%;--delay:-5s;"></span>
      <span class="ph-line" style="--top:85%;--delay:-7s;"></span>
    </div>
  </div>

  <div class="container ph-container">

    <!-- ─── Hero Manifesto Block ─────────────────────────── -->
    <div class="ph-hero" data-ph-hero>
      <div class="ph-eyebrow">
        <span class="ph-eyebrow__icon">
          <svg viewBox="0 0 24 24" fill="none" width="16" height="16">
            <path d="m13 2-2 7h7l-8 13 2-8H5l8-12Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </span>
        <span class="ph-eyebrow__text">{copy.eyebrow}</span>
        <span class="ph-eyebrow__line"></span>
      </div>

      <h2 class="ph-title">
        <span class="ph-title__line">{copy.titleLead}</span>
        <span class="ph-title__accent">{copy.titleAccent}</span>
      </h2>

      <p class="ph-lead">{copy.lead}</p>
    </div>

    <!-- ─── Energy Circuit Visualization ──────────────────── -->
    <div class="ph-circuit" data-ph-circuit>
      <div class="ph-circuit__visual">
        <!-- SVG Energy Circuit -->
        <svg class="ph-circuit__svg" viewBox="0 0 800 280" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <defs>
            <linearGradient id="circuitGrad" x1="0%" y1="50%" x2="100%" y2="50%">
              <stop offset="0%" stop-color="#1e99d3" stop-opacity="0.6"/>
              <stop offset="50%" stop-color="#159647" stop-opacity="0.6"/>
              <stop offset="100%" stop-color="#ff8c00" stop-opacity="0.6"/>
            </linearGradient>
            <filter id="circuitGlow">
              <feGaussianBlur stdDeviation="4" result="blur"/>
              <feComposite in="SourceGraphic" in2="blur" operator="over"/>
            </filter>
          </defs>
          <!-- Main horizontal path -->
          <path class="ph-circuit__path-bg" d="M0 140 Q100 140 150 80 Q200 20 300 60 Q400 100 400 140 Q400 180 500 220 Q600 260 650 200 Q700 140 800 140" stroke="rgba(255,255,255,0.05)" stroke-width="2" fill="none"/>
          <path class="ph-circuit__path" d="M0 140 Q100 140 150 80 Q200 20 300 60 Q400 100 400 140 Q400 180 500 220 Q600 260 650 200 Q700 140 800 140" stroke="url(#circuitGrad)" stroke-width="2.5" fill="none" filter="url(#circuitGlow)" data-ph-path/>
          <!-- Junction dots -->
          <circle class="ph-circuit__dot ph-circuit__dot--cnc" cx="300" cy="60" r="5" fill="#e63946"/>
          <circle class="ph-circuit__dot ph-circuit__dot--2" cx="400" cy="140" r="6" fill="#fff"/>
          <circle class="ph-circuit__dot ph-circuit__dot--3" cx="650" cy="200" r="5" fill="#ff8c00"/>
        </svg>

        <!-- Brand nodes positioned along the circuit -->
        <div class="ph-circuit__node ph-circuit__node--cnc" data-ph-node>
          <div class="ph-node__beacon ph-node__beacon--cnc"></div>
          <div class="ph-node__card">
            <img src="/logos/cnc-logo-white.svg" alt="CNC" width="72" height="36" loading="lazy" decoding="async"/>
            <span>{copy.circuit.cnc}</span>
          </div>
        </div>

        <div class="ph-circuit__node ph-circuit__node--tajaddod" data-ph-node>
          <div class="ph-node__beacon ph-node__beacon--white"></div>
          <div class="ph-node__card ph-node__card--center">
            <img src="/logos/tajaddod-icon.svg" alt={copy.mobileNodes[1].name} width="36" height="36" loading="lazy" decoding="async"/>
            <span>{copy.circuit.tajaddod}</span>
          </div>
        </div>

        <div class="ph-circuit__node ph-circuit__node--liper" data-ph-node>
          <div class="ph-node__beacon"></div>
          <div class="ph-node__card">
            <img src="/logos/liper-logo-white.svg" alt="Liper" width="72" height="36" loading="lazy" decoding="async"/>
            <span>{copy.circuit.liper}</span>
          </div>
        </div>
      </div>
    </div>

    <div class="ph-mobile-path" aria-label={copy.pathLabel}>
      {mobileNodes.map((node) => (
        <div class={`ph-mobile-node ${node.hub ? 'ph-mobile-node--hub' : ''}`} style={`--node-color: ${node.color};`}>
          <span class="ph-mobile-node__dot"></span>
          <div class="ph-mobile-node__card">
            <img src={node.logo} alt={node.name} width="72" height="36" loading="lazy" decoding="async" />
            <div>
              <strong>{node.name}</strong>
              <span>{node.text}</span>
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- ─── Philosophy Pillars ───────────────────────────── -->
    <div class="ph-pillars" data-ph-pillars>

      <div class="ph-pillar" data-ph-pillar data-brand="cnc">
        <div class="ph-pillar__accent" style="--pillar-color: var(--tj-green);"></div>
        <div class="ph-pillar__head">
          <div class="ph-pillar__icon" style="--pillar-color: var(--tj-green);">
            <svg viewBox="0 0 24 24" fill="none" width="24" height="24">
              <path d="M12 3 5 6v5c0 5 3 8.5 7 10 4-1.5 7-5 7-10V6l-7-3Zm0 6v6m0 3h.01" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <span class="ph-pillar__num">01</span>
        </div>
        <h3 class="ph-pillar__title">{copy.pillars[0].title}</h3>
        <p class="ph-pillar__text">{copy.pillars[0].text}</p>
        <div class="ph-pillar__brand">
          <img src="/logos/cnc-logo-white.svg" alt="CNC" width="72" height="36" loading="lazy" decoding="async"/>
        </div>
        <div class="ph-pillar__glow" style="--pillar-color: var(--tj-green);"></div>
      </div>

      <div class="ph-pillar" data-ph-pillar data-brand="tajaddod">
        <div class="ph-pillar__accent" style="--pillar-color: var(--tj-blue);"></div>
        <div class="ph-pillar__head">
          <div class="ph-pillar__icon" style="--pillar-color: var(--tj-blue);">
            <svg viewBox="0 0 24 24" fill="none" width="24" height="24">
              <path d="m13 2-2 7h7l-8 13 2-8H5l8-12Z" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <span class="ph-pillar__num">02</span>
        </div>
        <h3 class="ph-pillar__title">{copy.pillars[1].title}</h3>
        <p class="ph-pillar__text">{copy.pillars[1].text}</p>
        <div class="ph-pillar__brand">
          <img src="/logos/tajaddod-icon.svg" alt={copy.mobileNodes[1].name} width="18" height="18" loading="lazy" decoding="async"/>
        </div>
        <div class="ph-pillar__glow" style="--pillar-color: var(--tj-blue);"></div>
      </div>

      <div class="ph-pillar" data-ph-pillar data-brand="liper">
        <div class="ph-pillar__accent" style="--pillar-color: var(--tj-orange);"></div>
        <div class="ph-pillar__head">
          <div class="ph-pillar__icon" style="--pillar-color: var(--tj-orange);">
            <svg viewBox="0 0 24 24" fill="none" width="24" height="24">
              <path d="M9 21h6m-5-4h4m-7-6a5 5 0 1 1 10 0c0 2-1.5 3.2-2.7 4.2-.8.7-1.3 1.3-1.3 2.3h-2c0-1-.5-1.6-1.3-2.3C8.5 14.2 7 13 7 11Z" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <span class="ph-pillar__num">03</span>
        </div>
        <h3 class="ph-pillar__title">{copy.pillars[2].title}</h3>
        <p class="ph-pillar__text">{copy.pillars[2].text}</p>
        <div class="ph-pillar__brand">
          <img src="/logos/liper-logo-white.svg" alt="Liper" width="72" height="36" loading="lazy" decoding="async"/>
        </div>
        <div class="ph-pillar__glow" style="--pillar-color: var(--tj-orange);"></div>
      </div>

    </div>

    <!-- ─── Closing Statement ────────────────────────────── -->
    <div class="ph-closing" data-ph-closing>
      <div class="ph-closing__connector">
        <div class="ph-closing__dot"></div>
        <div class="ph-closing__line"></div>
        <div class="ph-closing__dot"></div>
      </div>
      <blockquote class="ph-quote">
        <span class="ph-quote__mark">"</span>
        <p>{copy.quote}</p>
        <span class="ph-quote__mark">"</span>
      </blockquote>
      <div class="ph-closing__brands">
        <img src="/logos/cnc-logo-white.svg" alt="CNC" width="72" height="36" loading="lazy" decoding="async"/>
        <span class="ph-closing__divider"></span>
        <img src="/logos/liper-logo-white.svg" alt="Liper" width="72" height="36" loading="lazy" decoding="async"/>
      </div>
    </div>

  </div>
</section>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';
  import { documentDirection, fromInlineStart } from '../../lib/direction';

  gsap.registerPlugin(ScrollTrigger);

  document.addEventListener('DOMContentLoaded', () => {
    const section = document.querySelector('.philosophy-section') as HTMLElement;
    if (!section) return;
    const isMobile = window.matchMedia('(max-width: 767px)').matches;
    const direction = documentDirection();

    // ─── Hero Manifesto Animation ──────────────────────────
    const hero = section.querySelector('[data-ph-hero]');
    if (hero) {
      const heroTl = gsap.timeline({
        scrollTrigger: {
          trigger: hero,
          start: 'top 80%',
          toggleActions: 'play none none reverse',
        }
      });

      heroTl
        .from(hero.querySelector('.ph-eyebrow'), {
          y: 30, opacity: 0, duration: 0.7, ease: 'power3.out'
        })
        .from(hero.querySelector('.ph-title__line'), {
          y: 50, opacity: 0, duration: 0.8, ease: 'power3.out'
        }, '-=0.3')
        .from(hero.querySelector('.ph-title__accent'), {
          y: 50, opacity: 0, duration: 0.8, ease: 'power3.out'
        }, '-=0.5')
        .from(hero.querySelector('.ph-lead'), {
          y: 30, opacity: 0, duration: 0.7, ease: 'power2.out'
        }, '-=0.4');
    }

    // ─── Circuit Animation ─────────────────────────────────
    const circuit = section.querySelector('[data-ph-circuit]');
    const svgPath = section.querySelector('[data-ph-path]') as SVGPathElement | null;

    if (circuit && svgPath && !isMobile) {
      const pathLength = svgPath.getTotalLength();
      gsap.set(svgPath, {
        strokeDasharray: pathLength,
        strokeDashoffset: pathLength
      });

      const circuitTl = gsap.timeline({
        scrollTrigger: {
          trigger: circuit,
          start: 'top 70%',
          end: 'bottom 40%',
          scrub: 1.5,
        }
      });

      circuitTl.to(svgPath, {
        strokeDashoffset: 0,
        ease: 'none'
      });

      // Dots appear as path reaches them
      const dots = section.querySelectorAll('.ph-circuit__dot');
      dots.forEach((dot, i) => {
        gsap.from(dot, {
          scale: 0,
          opacity: 0,
          duration: 0.5,
          ease: 'back.out(2)',
          scrollTrigger: {
            trigger: circuit,
            start: `top ${65 - i * 10}%`,
            toggleActions: 'play none none reverse',
          }
        });
      });

      // Nodes appear sequentially
      const nodes = section.querySelectorAll('[data-ph-node]');
      nodes.forEach((node, i) => {
        gsap.from(node, {
          y: 40,
          opacity: 0,
          scale: 0.8,
          duration: 0.7,
          ease: 'power3.out',
          scrollTrigger: {
            trigger: circuit,
            start: `top ${60 - i * 12}%`,
            toggleActions: 'play none none reverse',
          }
        });
      });
    }

    // ─── Pillars Stagger Animation ─────────────────────────
    const pillars = section.querySelectorAll('[data-ph-pillar]');
    if (pillars.length > 0) {
      pillars.forEach((pillar, i) => {
        const tl = gsap.timeline({
          scrollTrigger: {
            trigger: pillar,
            start: 'top 80%',
            toggleActions: 'play none none reverse',
          }
        });

        tl.from(pillar, {
          y: 60,
          opacity: 0,
          scale: 0.95,
          duration: 0.7,
          delay: i * 0.12,
          ease: 'power3.out'
        })
        .from(pillar.querySelector('.ph-pillar__icon'), {
          scale: 0,
          rotation: -90,
          duration: 0.5,
          ease: 'back.out(2)'
        }, '-=0.3')
        .from(pillar.querySelector('.ph-pillar__brand'), {
          x: fromInlineStart(20, direction),
          opacity: 0,
          duration: 0.4,
          ease: 'power2.out'
        }, '-=0.2');
      });
    }

    // ─── Closing Quote Animation ───────────────────────────
    const closing = section.querySelector('[data-ph-closing]');
    if (closing) {
      gsap.from(closing, {
        y: 40,
        opacity: 0,
        duration: 0.8,
        ease: 'power3.out',
        scrollTrigger: {
          trigger: closing,
          start: 'top 85%',
          toggleActions: 'play none none reverse',
        }
      });
    }

    // ─── Pillar Hover Parallax ─────────────────────────────
    if (!isMobile) {
      pillars.forEach(pillar => {
        const glow = pillar.querySelector('.ph-pillar__glow') as HTMLElement;
        if (!glow) return;

        pillar.addEventListener('mousemove', (e: Event) => {
          const me = e as MouseEvent;
          const rect = (pillar as HTMLElement).getBoundingClientRect();
          const x = ((me.clientX - rect.left) / rect.width - 0.5) * 2;
          const y = ((me.clientY - rect.top) / rect.height - 0.5) * 2;
          gsap.to(glow, {
            x: x * 30,
            y: y * 30,
            duration: 0.4,
            ease: 'power2.out'
          });
        });

        pillar.addEventListener('mouseleave', () => {
          gsap.to(glow, { x: 0, y: 0, duration: 0.6, ease: 'power2.out' });
        });
      });
    }
  });
</script>

<style>
/* ═══════════════════════════════════════════════════════════════════════
   Philosophy Section — فلسفة تجدد
   Premium cinematic design with circuit visualization
   ═══════════════════════════════════════════════════════════════════════ */

.philosophy-section {
  position: relative;
  overflow: hidden;
  padding: clamp(100px, 12vw, 180px) 0 !important;
  background: linear-gradient(
    160deg,
    #040c14 0%,
    #071e2a 20%,
    #0a3020 45%,
    #082838 70%,
    #040c14 100%
  ) !important;
  color: var(--tj-text-inverse);
}

/* ─── Background Effects ───────────────────────────────────── */

.ph-bg {
  position: absolute;
  inset: 0;
  pointer-events: none;
  z-index: 0;
}

.ph-bg__grid {
  position: absolute;
  inset: 0;
  opacity: 0.025;
  background-image:
    linear-gradient(var(--tj-inverse-alpha-06) 1px, transparent 1px),
    linear-gradient(90deg, var(--tj-inverse-alpha-06) 1px, transparent 1px);
  background-size: 80px 80px;
  mask-image: radial-gradient(ellipse at 50% 30%, var(--tj-neutral-950) 10%, transparent 65%);
  -webkit-mask-image: radial-gradient(ellipse at 50% 30%, var(--tj-neutral-950) 10%, transparent 65%);
}

.ph-bg__aurora {
  position: absolute;
  border-radius: 50%;
  filter: blur(140px);
  opacity: 0.08;
  animation: phAurora 14s ease-in-out infinite alternate;
}

.ph-bg__aurora--1 {
  width: 700px;
  height: 500px;
  top: -10%;
  right: -5%;
  background: var(--tj-blue);
}

.ph-bg__aurora--2 {
  width: 500px;
  height: 600px;
  bottom: -5%;
  left: -8%;
  background: var(--tj-green);
  animation-delay: -5s;
}

.ph-bg__aurora--3 {
  width: 400px;
  height: 350px;
  top: 50%;
  left: 40%;
  background: var(--tj-orange);
  animation-delay: -9s;
  opacity: 0.04;
}

@keyframes phAurora {
  0% { transform: translate(0, 0) scale(1); }
  50% { transform: translate(25px, -15px) scale(1.12); }
  100% { transform: translate(-15px, 10px) scale(0.95); }
}

.ph-bg__noise {
  position: absolute;
  inset: 0;
  opacity: 0.03;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
}

/* Energy lines */
.ph-bg__lines {
  position: absolute;
  inset: 0;
  overflow: hidden;
}

.ph-line {
  position: absolute;
  top: var(--top);
  right: -20%;
  height: 1.5px;
  width: 35vw;
  background: linear-gradient(90deg, transparent, color-mix(in srgb, var(--tj-orange) 25%, transparent), color-mix(in srgb, var(--tj-blue) 20%, transparent), transparent);
  filter: drop-shadow(0 0 8px color-mix(in srgb, var(--tj-orange) 30%, transparent));
  animation: phLineFlow 9s linear var(--delay) infinite;
  opacity: 0.4;
}

@keyframes phLineFlow {
  0% { transform: translateX(0); opacity: 0; }
  10% { opacity: 0.4; }
  90% { opacity: 0.4; }
  100% { transform: translateX(-120vw); opacity: 0; }
}

/* ─── Container ────────────────────────────────────────────── */

.ph-container {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: clamp(60px, 8vw, 100px);
}

/* ─── Hero Manifesto ───────────────────────────────────────── */

.ph-hero {
  text-align: center;
  max-width: 780px;
}

.ph-eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 28px;
  padding: 8px 20px 8px 14px;
  background: var(--tj-inverse-alpha-03);
  border: 1px solid var(--tj-inverse-alpha-08);
  border-radius: 999px;
}

.ph-eyebrow__icon {
  display: grid;
  place-items: center;
  width: 28px;
  height: 28px;
  border-radius: 50%;
  background: linear-gradient(135deg, color-mix(in srgb, var(--tj-blue) 20%, transparent), color-mix(in srgb, var(--tj-orange) 20%, transparent));
  color: var(--tj-blue-light);
}

.ph-eyebrow__text {
  color: var(--tj-blue-light);
  font-size: var(--tj-text-small);
  font-weight: var(--tj-font-semibold);
  letter-spacing: 0.06em;
}

.ph-eyebrow__line {
  width: 40px;
  height: 1px;
  background: linear-gradient(90deg, rgba(155,232,255,0.4), transparent);
}

.ph-title {
  color: var(--tj-text-inverse);
  font-size: clamp(1.7rem, 4vw, 2.7rem);
  font-weight: var(--tj-font-extrabold);
  line-height: 1.25;
  margin-bottom: 28px;
}

.ph-title__line {
  display: block;
  margin-bottom: 6px;
}

.ph-title__accent {
  display: block;
  background: linear-gradient(135deg, var(--tj-blue-light) 0%, var(--tj-blue) 30%, var(--tj-green-light) 70%, var(--tj-blue-light) 100%);
  background-size: 250% auto;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  animation: phGradientShift 8s ease-in-out infinite;
}

@keyframes phGradientShift {
  0%, 100% { background-position: 0% center; }
  50% { background-position: 100% center; }
}

.ph-lead {
  color: var(--tj-inverse-alpha-55);
  font-size: var(--tj-text-body-lg);
  line-height: 1.95;
  max-width: 660px;
  margin: 0 auto;
}

/* ─── Circuit Visualization ────────────────────────────────── */

.ph-circuit {
  width: 100%;
  max-width: 800px;
  position: relative;
}

.ph-mobile-path {
  display: none;
}

.ph-circuit__visual {
  position: relative;
  width: 100%;
  padding-bottom: 35%;
}

.ph-circuit__svg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
}

.ph-circuit__path-bg {
  opacity: 0.5;
}

.ph-circuit__path {
  transition: stroke 0.4s ease;
}

.ph-circuit__dot {
  filter: drop-shadow(0 0 8px currentColor);
}

.ph-circuit__dot--2 {
  filter: drop-shadow(0 0 12px var(--tj-inverse-alpha-60));
}

/* Circuit Nodes */
.ph-circuit__node {
  position: absolute;
  z-index: 3;
}

.ph-circuit__node--cnc {
  top: -15%;
  left: 31%;
}

.ph-circuit__node--tajaddod {
  top: 15%;
  left: 50%;
  transform: translateX(-50%);
}

.ph-circuit__node--liper {
  bottom: -15%;
  right: 12%;
}

.ph-node__beacon {
  position: absolute;
  inset: -8px;
  border-radius: 50%;
  background: var(--tj-blue);
  opacity: 0;
  animation: phBeacon 2.5s ease-out infinite;
  z-index: 0;
}

.ph-circuit__node--cnc .ph-node__beacon {
  background: var(--tj-cnc-red);
}

.ph-circuit__node--liper .ph-node__beacon {
  background: var(--tj-orange);
}

.ph-node__beacon--white {
  background: var(--tj-inverse-alpha-30) !important;
}

@keyframes phBeacon {
  0% { transform: scale(0.5); opacity: 0.5; }
  100% { transform: scale(3); opacity: 0; }
}

.ph-node__card {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  padding: 14px 20px;
  background: var(--tj-inverse-alpha-05);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid var(--tj-inverse-alpha-10);
  border-radius: 18px;
  box-shadow: 0 12px 40px color-mix(in srgb, var(--tj-neutral-950) 25%, transparent);
  position: relative;
  z-index: 1;
  transition: transform 0.4s var(--tj-ease-smooth), box-shadow 0.4s var(--tj-ease-smooth);
}

.ph-node__card:hover {
  transform: translateY(-4px);
  box-shadow: 0 20px 50px color-mix(in srgb, var(--tj-neutral-950) 35%, transparent);
}

.ph-node__card--center {
  background: var(--tj-inverse-alpha-08);
  border-color: var(--tj-inverse-alpha-15);
}

.ph-node__card img {
  height: 26px;
  width: auto;
  object-fit: contain;
  opacity: 0.85;
}

.ph-node__card span {
  color: var(--tj-inverse-alpha-60);
  font-size: var(--tj-text-nano);
  font-weight: var(--tj-font-semibold);
  white-space: nowrap;
}

/* ─── Philosophy Pillars ───────────────────────────────────── */

.ph-pillars {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 22px;
  width: 100%;
}

.ph-pillar {
  position: relative;
  overflow: hidden;
  padding: 36px 30px 30px;
  background: var(--tj-inverse-alpha-02);
  backdrop-filter: blur(24px);
  -webkit-backdrop-filter: blur(24px);
  border: 1px solid var(--tj-inverse-alpha-06);
  border-radius: 26px;
  transition:
    transform 0.5s var(--tj-ease-smooth),
    border-color 0.5s var(--tj-ease-smooth),
    box-shadow 0.5s var(--tj-ease-smooth);
  cursor: default;
}

.ph-pillar:hover {
  transform: translateY(-8px);
  border-color: var(--tj-inverse-alpha-15);
  box-shadow:
    0 30px 70px color-mix(in srgb, var(--tj-neutral-950) 25%, transparent),
    inset 0 1px 0 var(--tj-inverse-alpha-06);
}

/* Top accent line */
.ph-pillar__accent {
  position: absolute;
  top: 0;
  left: 30px;
  right: 30px;
  height: 2px;
  background: linear-gradient(90deg, transparent, var(--pillar-color), transparent);
  opacity: 0;
  transition: opacity 0.5s ease;
}

.ph-pillar:hover .ph-pillar__accent {
  opacity: 0.6;
}

/* Pillar head */
.ph-pillar__head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 22px;
}

.ph-pillar__icon {
  display: grid;
  place-items: center;
  width: 52px;
  height: 52px;
  border-radius: 16px;
  background: color-mix(in srgb, var(--pillar-color) 10%, transparent);
  border: 1px solid color-mix(in srgb, var(--pillar-color) 20%, transparent);
  color: var(--pillar-color);
  transition: all 0.4s var(--tj-ease-smooth);
}

.ph-pillar:hover .ph-pillar__icon {
  background: color-mix(in srgb, var(--pillar-color) 18%, transparent);
  border-color: color-mix(in srgb, var(--pillar-color) 35%, transparent);
  box-shadow: 0 0 24px color-mix(in srgb, var(--pillar-color) 20%, transparent);
}

.ph-pillar__num {
  color: var(--tj-inverse-alpha-04);
  font-size: 3.5rem;
  font-weight: var(--tj-font-black);
  line-height: 1;
  transition: color 0.5s ease;
}

.ph-pillar:hover .ph-pillar__num {
  color: var(--tj-inverse-alpha-08);
}

.ph-pillar__title {
  color: var(--tj-text-inverse);
  font-size: 1.3rem;
  font-weight: var(--tj-font-bold);
  margin-bottom: 14px;
}

.ph-pillar__text {
  color: var(--tj-inverse-alpha-50);
  font-size: var(--tj-text-body);
  line-height: 1.9;
  margin-bottom: 24px;
  transition: color 0.4s ease;
}

.ph-pillar:hover .ph-pillar__text {
  color: var(--tj-inverse-alpha-65);
}

/* Brand tag */
.ph-pillar__brand {
  display: inline-flex;
  align-items: center;
  padding: 10px 16px;
  background: var(--tj-inverse-alpha-04);
  border: 1px solid var(--tj-inverse-alpha-06);
  border-radius: 14px;
  transition: all 0.4s ease;
}

.ph-pillar:hover .ph-pillar__brand {
  background: var(--tj-inverse-alpha-07);
  border-color: var(--tj-inverse-alpha-12);
}

.ph-pillar__brand img {
  height: 22px;
  width: auto;
  object-fit: contain;
  opacity: 0.45;
  transition: opacity 0.4s ease;
}

.ph-pillar:hover .ph-pillar__brand img {
  opacity: 0.85;
}

/* Glow effect */
.ph-pillar__glow {
  position: absolute;
  width: 200px;
  height: 200px;
  border-radius: 50%;
  background: var(--pillar-color);
  filter: blur(80px);
  opacity: 0;
  bottom: -60px;
  right: -40px;
  transition: opacity 0.6s ease;
  pointer-events: none;
}

.ph-pillar:hover .ph-pillar__glow {
  opacity: 0.15;
}

/* ─── Closing Statement ────────────────────────────────────── */

.ph-closing {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 28px;
  max-width: 700px;
  width: 100%;
}

.ph-closing__connector {
  display: flex;
  align-items: center;
  gap: 0;
  width: 80%;
}

.ph-closing__dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--tj-blue);
  opacity: 0.4;
  flex-shrink: 0;
}

.ph-closing__line {
  flex: 1;
  height: 1px;
  background: linear-gradient(90deg, var(--tj-blue), var(--tj-inverse-alpha-10), var(--tj-green));
  opacity: 0.2;
}

.ph-quote {
  display: flex;
  align-items: flex-start;
  gap: 8px;
  text-align: center;
  margin: 0;
  padding: 28px 32px;
  background: var(--tj-inverse-alpha-02);
  border: 1px solid var(--tj-inverse-alpha-05);
  border-radius: 22px;
  position: relative;
}

.ph-quote p {
  color: var(--tj-inverse-alpha-55);
  font-size: var(--tj-text-body-lg);
  line-height: 1.9;
  font-style: italic;
  margin: 0;
}

.ph-quote__mark {
  color: color-mix(in srgb, var(--tj-blue) 30%, transparent);
  font-size: 2.5rem;
  font-weight: var(--tj-font-bold);
  line-height: 0.8;
  font-style: normal;
  flex-shrink: 0;
}

.ph-closing__brands {
  display: flex;
  align-items: center;
  gap: 20px;
}

.ph-closing__brands img {
  height: 20px;
  width: auto;
  object-fit: contain;
  opacity: 0.35;
  transition: opacity 0.4s ease;
}

.ph-closing__brands img:hover {
  opacity: 0.8;
}

.ph-closing__divider {
  width: 1px;
  height: 16px;
  background: var(--tj-inverse-alpha-10);
}

/* ═══════════════════════════════════════════════════════════════════════
   Responsive
   ═══════════════════════════════════════════════════════════════════════ */

@media (max-width: 1100px) {
  .ph-pillars {
    grid-template-columns: 1fr;
    max-width: 520px;
    margin: 0 auto;
  }

  .ph-circuit__visual {
    padding-bottom: 45%;
  }

  .ph-circuit__node--liper {
    right: 6%;
  }
}

@media (max-width: 767px) {
  .philosophy-section {
    padding: clamp(4rem, 12vw, 6rem) 0 !important;
  }

  .ph-container {
    gap: 34px;
  }

  .ph-circuit {
    display: none;
  }

  .ph-mobile-path {
    position: relative;
    display: grid;
    gap: 14px;
    width: min(100%, 430px);
    margin-inline: auto;
    padding: 8px 0;
  }

  .ph-mobile-path::before {
    content: '';
    position: absolute;
    top: 24px;
    bottom: 24px;
    right: 23px;
    width: 2px;
    border-radius: 999px;
    background: linear-gradient(180deg, var(--tj-blue), var(--tj-inverse-alpha-30), var(--tj-green), var(--tj-green-light));
    box-shadow: 0 0 18px color-mix(in srgb, var(--tj-blue) 32%, transparent);
    opacity: .72;
  }

  .ph-mobile-node {
    position: relative;
    display: grid;
    grid-template-columns: 48px 1fr;
    align-items: center;
    gap: 12px;
  }

  .ph-mobile-node__dot {
    position: relative;
    z-index: 2;
    display: grid;
    place-items: center;
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: rgba(2, 11, 18, .92);
    border: 1px solid color-mix(in srgb, var(--node-color) 52%, var(--tj-inverse-alpha-20));
    box-shadow: 0 0 22px color-mix(in srgb, var(--node-color) 28%, transparent);
  }

  .ph-mobile-node__dot::after {
    content: '';
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--node-color);
    box-shadow: 0 0 14px var(--node-color);
  }

  .ph-mobile-node--hub .ph-mobile-node__dot {
    width: 58px;
    height: 58px;
    transform: translateX(5px);
    background: linear-gradient(135deg, color-mix(in srgb, var(--tj-blue) 24%, transparent), color-mix(in srgb, var(--tj-orange) 15%, transparent)), var(--tj-inverse-alpha-08);
  }

  .ph-mobile-node--hub .ph-mobile-node__dot::after {
    width: 16px;
    height: 16px;
    background: var(--tj-bg-surface);
  }

  .ph-mobile-node__card {
    display: flex;
    align-items: center;
    gap: 12px;
    min-height: 70px;
    padding: 12px 14px;
    border: 1px solid var(--tj-inverse-alpha-08);
    border-radius: 18px;
    background: var(--tj-inverse-alpha-04);
    box-shadow: 0 18px 40px color-mix(in srgb, var(--tj-neutral-950) 18%, transparent), inset 0 1px 0 var(--tj-inverse-alpha-05);
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
  }

  .ph-mobile-node--hub .ph-mobile-node__card {
    border-color: var(--tj-inverse-alpha-15);
    background: var(--tj-inverse-alpha-08);
  }

  .ph-mobile-node__card img {
    width: 42px;
    height: 28px;
    object-fit: contain;
    flex-shrink: 0;
  }

  .ph-mobile-node--hub .ph-mobile-node__card img {
    width: 34px;
    height: 34px;
  }

  .ph-mobile-node__card strong {
    display: block;
    color: var(--tj-text-inverse);
    font-size: .92rem;
    line-height: 1.2;
  }

  .ph-mobile-node__card span {
    display: block;
    color: var(--tj-inverse-alpha-60);
    font-size: .76rem;
    line-height: 1.55;
  }

  .ph-hero {
    max-width: 100%;
  }

  .ph-eyebrow {
    padding: 6px 16px 6px 10px;
  }

  .ph-title {
    font-size: clamp(1.45rem, 5vw, 2rem);
  }

  .ph-lead {
    font-size: .95rem;
    line-height: 1.8;
  }

  .ph-pillars {
    gap: 12px;
    max-width: 430px;
  }

  .ph-pillar {
    padding: 18px;
    border-radius: 20px;
  }

  .ph-pillar:hover {
    transform: none;
  }

  .ph-pillar__head {
    margin-bottom: 12px;
  }

  .ph-pillar__icon {
    width: 44px;
    height: 44px;
    border-radius: 14px;
  }

  .ph-pillar__num {
    font-size: 2.4rem;
  }

  .ph-pillar__title {
    margin-bottom: 8px;
    font-size: 1.05rem;
  }

  .ph-pillar__text {
    display: -webkit-box;
    margin-bottom: 14px;
    overflow: hidden;
    color: var(--tj-inverse-alpha-60);
    font-size: .88rem;
    line-height: 1.65;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
  }

  .ph-pillar__brand {
    padding: 8px 12px;
    border-radius: 12px;
  }

  .ph-closing__connector {
    width: 60%;
  }

  .ph-quote {
    padding: 22px 24px;
  }

  .ph-closing__brands img {
    height: 16px;
  }
}

@media (max-width: 359px) {
  .ph-pillar {
    padding: 28px 22px 24px;
    border-radius: 22px;
  }

  .ph-pillar__icon {
    width: 44px;
    height: 44px;
    border-radius: 14px;
  }

  .ph-pillar__num {
    font-size: 2.8rem;
  }

  .ph-eyebrow__line {
    display: none;
  }

  .ph-closing__brands {
    gap: 14px;
  }

  .ph-quote__mark {
    font-size: 2rem;
  }
}
</style>
