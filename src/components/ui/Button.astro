---
interface Props {
  href: string;
  variant?: 'primary' | 'secondary' | 'ghost' | 'dark';
  label?: string;
}
const { href, variant = 'primary', label } = Astro.props;
---
<a class={`button button-${variant}`} href={href} aria-label={label}>
  <slot />
</a>
